---
ansible:
  playbook: tests/playbook.yml

driver:
  name: docker

docker:
  containers:
    - name: amazonlinux
      image: amazonlinux
      image_version: latest
    - name: centos
      image: centos
      image_version: latest
    - name: ubuntu
      image: ubuntu
      image_version: latest

#verifier:
#  name: testinfra
